<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <title>Movie</title>
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh; /* Prend toute la hauteur de la fenêtre */
            padding-top: 125px; /* Hauteur de la navbar pour éviter que le contenu soit caché */
            padding-bottom: 125px; /* Hauteur de la navbar pour éviter que le contenu soit caché */

        }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
    <a class="navbar-brand" href="#">Mon Site</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('Homepage') }}">Go to Homepage</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('Movie') }}">Go to Movie</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('Authors') }}">See Authors</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('Books') }}">Their Books</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('Register') }}">Register</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                {% if app.user %}
                    <a style="font-size: x-large" class="nav-link"><strong>Bienvenue {{ app.user.username }}</strong></a>
                    <a href="{{ path('app_logout') }}"><center>Logout</center></a>
                {% else %}
                    <a style="font-size: x-large" class="nav-link"><strong>Connecté en tant qu'invité</strong></a>
                    <a href="{{ path('Login') }}"><center>Login</center></a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>
</body>
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</html>
